name: 'local'

path:
  templates: ./templates
  plugins: ./plugins
  destination: ./

assets:
  images:
    source: ./img
  css:
    source: ./css/main.scss
    processor:
      name: sass
      hash: true
